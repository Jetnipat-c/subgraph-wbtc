{
  "name": "subgraph-wbtc",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph-wbtc",
    "create-local": "graph create --node http://localhost:8020/ subgraph-wbtc",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph-wbtc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph-wbtc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
